<%= link_to "&lt; Back", :back %>
<h2> <%= @title %> </h2>

<%=image_tag 'feed-icon-14x14.png' -%> <%= link_to 'RSS Feed' , 'http://'+ request.host_with_port + ActionController::Base.relative_url_root + request.path + '.rss' %>&nbsp;
<% if @linktomap %><%= link_to "Show Just Map Activity", :controller=>"audits", :action=>"for_map_model" %><% end %>
<br /><br />

<%= will_paginate @audits %>

<%= render :partial => 'audit_table', :object => @audits %>

<%= will_paginate @audits %>
